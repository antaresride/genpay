name: CI/CD & Release

on:
    push:
        branches: ["main"]
        tags: ["v*"]
    pull_request:
        branches: ["main"]

permissions:
    contents: write

jobs:
    lint:
        name: Lint & Format
        runs-on: 
        env:
            # Required for llvm-sys crate to find LLVM 21
            LLVM_SYS_211_PREFIX: /usr/lib/llvm-21
            LIBCLANG_PATH: /usr/lib/llvm-21/lib
        steps:
            - uses: actions/checkout@v4

            - name: Install LLVM 21 (Ubuntu)
              run: |
                  sudo apt-get update
                  sudo apt-get install -y wget lsb-release software-properties-common gnupg curl libz-dev libedit-dev libxml2-dev
                  curl -L https://apt.llvm.org/llvm.sh -o llvm.sh
                  chmod +x llvm.sh
                  sudo ./llvm.sh 21 all
                  sudo ln -sf /usr/lib/llvm-21/bin/llvm-config /usr/bin/llvm-config

            - uses: dtolnay/rust-toolchain@stable
              with:
                  components: rustfmt, clippy

            - name: Check Formatting
              run: cargo fmt --all -- --check

            - name: Clippy
              run: cargo clippy --workspace -- -D warnings

    test-and-build:
        name: ${{ matrix.platform }}
        needs: lint
        strategy:
            fail-fast: false
            matrix:
                include:
                    - os: ubuntu-24.04-arm
                      target: aarch64-unknown-linux-gnu
                      platform: linux-aarch64
                    - os: macos-15
                      target: aarch64-apple-darwin
                      platform: macos-aarch64
        runs-on: ${{ matrix.os }}

        steps:
            - name: Checkout Code
              uses: actions/checkout@v4

            - name: Install Rust
              uses: dtolnay/rust-toolchain@stable
              with:
                  targets: ${{ matrix.target }}

            - name: Install LLVM 21 (Ubuntu ARM)
              if: startsWith(matrix.os, 'ubuntu')
              run: |
                  sudo apt-get update
                  sudo apt-get install -y wget lsb-release software-properties-common gnupg curl libz-dev libedit-dev libxml2-dev
                  curl -L https://apt.llvm.org/llvm.sh -o llvm.sh
                  chmod +x llvm.sh
                  sudo ./llvm.sh 21 all
                  sudo ln -sf /usr/lib/llvm-21/bin/llvm-config /usr/bin/llvm-config
                  echo "LLVM_SYS_211_PREFIX=/usr/lib/llvm-21" >> $GITHUB_ENV
                  echo "LIBCLANG_PATH=/usr/lib/llvm-21/lib" >> $GITHUB_ENV

            - name: Install LLVM 21 (macOS)
              if: startsWith(matrix.os, 'macos')
              run: |
                  brew install llvm@21
                  echo "LLVM_SYS_211_PREFIX=$(brew --prefix llvm@21)" >> $GITHUB_ENV
                  echo "LIBCLANG_PATH=$(brew --prefix llvm@21)/lib" >> $GITHUB_ENV
                  echo "$(brew --prefix llvm@21)/bin" >> $GITHUB_PATH

            - name: Rust Cache
              uses: Swatinem/rust-cache@v2
              with:
                  key: ${{ matrix.target }}-llvm-21-v5

            - name: Run Tests
              run: cargo test --workspace --target ${{ matrix.target }}

            - name: Build Release Binary
              if: startsWith(github.ref, 'refs/tags/v')
              run: cargo build --release --target ${{ matrix.target }} --bin genpay

            - name: Prepare Release Assets
              if: startsWith(github.ref, 'refs/tags/v')
              shell: bash
              run: |
                  cp target/${{ matrix.target }}/release/genpay genpay-${{ matrix.platform }}
                  if command -v sha256sum >/dev/null; then
                    sha256sum genpay-${{ matrix.platform }} > genpay-${{ matrix.platform }}.sha256
                  else
                    shasum -a 256 genpay-${{ matrix.platform }} > genpay-${{ matrix.platform }}.sha256
                  fi

            - name: Upload Artifact
              if: startsWith(github.ref, 'refs/tags/v')
              uses: actions/upload-artifact@v4
              with:
                  name: genpay-${{ matrix.platform }}
                  path: |
                      genpay-${{ matrix.platform }}
                      genpay-${{ matrix.platform }}.sha256

    create-release:
        name: Create Release
        needs: test-and-build
        if: startsWith(github.ref, 'refs/tags/v')
        runs-on: ubuntu-latest
        steps:
            - name: Download All Artifacts
              uses: actions/download-artifact@v4
              with:
                  path: ./artifacts
                  pattern: genpay-*
                  merge-multiple: true

            - name: Create GitHub Release
              uses: softprops/action-gh-release@v2
              with:
                  files: ./artifacts/*
                  body: |
                      ## Installation

                      ### Linux (ARM64)

                      ```bash
                      cd ~/Downloads
                      curl -LO "https://github.com/antaresride/genpay/releases/download/${{ github.ref_name }}/genpay-linux-aarch64"
                      curl -LO "https://github.com/antaresride/genpay/releases/download/${{ github.ref_name }}/genpay-linux-aarch64.sha256"
                      sha256sum -c genpay-linux-aarch64.sha256
                      chmod +x genpay-linux-aarch64
                      sudo mv genpay-linux-aarch64 /usr/local/bin/genpay
                      genpay --help
                      ```

                      ### macOS (Apple Silicon)

                      ```bash
                      cd ~/Downloads
                      curl -LO "https://github.com/antaresride/genpay/releases/download/${{ github.ref_name }}/genpay-macos-aarch64"
                      curl -LO "https://github.com/antaresride/genpay/releases/download/${{ github.ref_name }}/genpay-macos-aarch64.sha256"
                      shasum -a 256 -c genpay-macos-aarch64.sha256
                      xattr -d com.apple.quarantine genpay-macos-aarch64 2>/dev/null || true
                      chmod +x genpay-macos-aarch64
                      sudo mv genpay-macos-aarch64 /usr/local/bin/genpay
                      genpay --help
                      ```
                  generate_release_notes: true
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

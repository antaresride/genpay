[package]
name = "linker"
version = "0.1.0"
edition = "2024"

[dependencies]
cranelift-module = { workspace = true }
cranelift-object = { workspace = true }
target-lexicon = { workspace = true }
inkwell = { workspace = true }
# Optional: only if it needs raw FFI
# llvm-sys = { workspace = true }

